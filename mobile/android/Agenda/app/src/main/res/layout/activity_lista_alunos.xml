<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/lista_alunos"/>

    <Button
        android:id="@+id/novo_aluno"
        android:layout_height="56dp"
        android:layout_width="56dp"
        android:text="+"
        android:textColor="#fff"
        android:textSize="40sp"
        android:elevation="6dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:background="@drawable/fundo"
        android:stateListAnimator="@null"/>

</RelativeLayout>

<!--
    MATCH PARENT -> indica que o componente usara a medida que o pai possui, no nosso caso, o parent
    é a própria tela, então o linear layout usa a tela toda para se posicionar
-->

<!--
    LINEAR LAYOUT -> Posiciona os elementos um a frente do outro, ou abaixo, dependendo da
    ORIENTATION informada
-->

<!--
    WRAP CONTENT -> Adiciona a medida suficiente para 'cobrir' o elemento
-->

<!--
    @+id/lista_alunos
        @ -> estamos acessando os recursos do android (pasta res)
        + -> estamos criando alguma coisa
            id -> estamos criando um id
                /texto -> valor do id
-->

<!--
    Todas as tags são componentes, classe que possuem suas respectivas views
-->